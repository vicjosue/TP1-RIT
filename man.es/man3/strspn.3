STRSPN(3)        Manual del Programador de Linux        STRSPN(3)



NOMBRE
       strspn,  strcspn  - busca un conjunto de caracteres en una
       cadena

SINOPSIS
       #include <string.h>

       size_t strspn(const char *s, const char *acepta);

       size_t strcspn(const char *s, const char *rechaza);

DESCRIPCIÓN
       La función strspn() calcula la longitud del segmento  ini­
       cial de s que consta únicamente de caracteres en acepta.

       La función strcspn() calcula la longitud del segmento ini­
       cial de s que  consta  únicamente  de  caracteres  que  no
       aparecen en rechaza.

VALOR DEVUELTO
       La  función  strspn() devuelve el número de caracteres del
       segmento  inicial  de  s  que  contiene  sólo   caracteres
       perteneciantes a acepta.

       La  función strcspn() devuelve el número de caracteres del
       segmento inicial de s que consta únicamente de  caracteres
       que no están en rechaza.

CONFORME A
       SVID 3, POSIX, BSD 4.3, ISO 9899

VÉASE TAMBIÉN
       index(3),  memchr(3),  rindex(3),  strchr(3),  strpbrk(3),
       strsep(3), strstr(3), strtok(3)



                       12 de Abril de 1993              STRSPN(3)
