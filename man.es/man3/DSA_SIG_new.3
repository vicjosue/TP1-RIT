DSA_SIG_new(3)               OpenSSL               DSA_SIG_new(3)



NNAAMMEE
       DSA_SIG_new, DSA_SIG_free - allocate and free DSA
       signature objects

SSYYNNOOPPSSIISS
        #include <openssl/dsa.h>

        DSA_SIG *DSA_SIG_new(void);

        void   DSA_SIG_free(DSA_SIG *a);


DDEESSCCRRIIPPTTIIOONN
       DSA_SIG_new() allocates and initializes a DDSSAA_SSIIGG
       structure.

       DSA_SIG_free() frees the DDSSAA_SSIIGG structure and its
       components. The values are erased before the memory is
       returned to the system.

RREETTUURRNN VVAALLUUEESS
       If the allocation fails, DSA_SIG_new() returns NNUULLLL and
       sets an error code that can be obtained by
       ERR_get_error(3). Otherwise it returns a pointer to the
       newly allocated structure.

       DSA_SIG_free() returns no value.

SSEEEE AALLSSOO
       dsa(3), err(3), DSA_do_sign(3)

HHIISSTTOORRYY
       DSA_SIG_new() and DSA_SIG_free() were added in OpenSSL
       0.9.3.











27/Jan/2000                   0.9.6c               DSA_SIG_new(3)
