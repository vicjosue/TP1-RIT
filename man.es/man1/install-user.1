INSTALL-USER(1)                                   INSTALL-USER(1)



NAME
       install-user  -  reads  or  installs a User Name in a Palm
       Device

SYNOPSIS
       install-user /dev/cua? [username]

DESCRIPTION
       This manual page documents the small application  install-
       user.   install-user allows the user to read or change the
       User Name on the Pilot connected to the specified  callout
       device.   Once executing install-user the user must invoke
       HotSync on the target device.

   OPTIONS
       none

USEAGE
       The program will connect to a target, and read install the
       User Name given on the command line, or read the User Name
       if none is specified.

BUGS
       install-user has no known bugs.

AUTHOR
       install-user was written by D. Jeff Dionne



FSF                Palm Computing Device Tools    INSTALL-USER(1)
